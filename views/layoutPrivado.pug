

extends ./layout.pug


block content


  nav.navbar.navbar-expand-lg.navbar-dark.sticky-top(style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); box-shadow: 0 2px 15px rgba(0,0,0,0.3);")


    .container-fluid


      a.navbar-brand(href='/home') Fotaza

      button.navbar-toggler(
        type='button'
        data-bs-toggle='collapse'
        data-bs-target='#navbarNavAltMarkup'
        aria-controls='navbarNavAltMarkup'
        aria-expanded='false'
        aria-label='Toggle navigation'
      )

        span.navbar-toggler-icon

      #navbarNavAltMarkup.collapse.navbar-collapse

        .navbar-nav.me-auto
        

          a.nav-link(href='/home') Home
          a.nav-link(href='/perfil') Mi Perfil
          a.nav-link(href='/publicaciones/crear') Crear Publicación
          a.nav-link(href='/usuarios/seguidores') Seguidores: #{cantidadSeguidores}
          a.nav-link(href='/usuarios/siguiendo') Siguiendo: #{cantidadSiguiendo}
          a.nav-link(href='/usuarios/siguiendo/publicaciones') Novedades
          a.nav-link(href='#') Favoritos
          a.nav-link(href='#') Notificaciones

        form.d-flex.mx-auto.gap-2(
          action='/buscador'
          method='GET'
        )

          input.form-control(
            type='search'
            name='texto'
            placeholder='Buscar publicación'
          )



          input.form-control(
            type='search'
            name='usuario'
            placeholder='Buscar usuario'
          )



          input.form-control(
            type='search'
            name='etiqueta'
            placeholder='Buscar etiqueta'
          )




          button.btn.btn-outline-light(type='submit')
            | Buscar

        .navbar-nav.ms-auto

          a.nav-link(href='/auth/logout')
            | Cerrar sesión

  block contenido